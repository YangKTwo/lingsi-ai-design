{
  "name": "lingsi-ai-design",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "pnpm -C apps/web dev",
    "dev:api": "cd services/api-server && venv\\Scripts\\activate && uvicorn main:app --reload --port 8000",
    "build:web": "pnpm -C apps/web build"
  }
}